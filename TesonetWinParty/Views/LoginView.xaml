<UserControl x:Class="TesonetWinParty.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TesonetWinParty.Views"
             mc:Ignorable="d" Background="White" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center">
            <TextBox x:Name="UserName" MinWidth="100" Margin="0 0 0 10"/>
            <PasswordBox x:Name="Password" MinWidth="100" Margin="0 0 0 10"/>
            <Button x:Name="LogIn">Login</Button>
        </StackPanel>
    </Grid>
</UserControl>
